@model GloboTicket.Frontend.Controllers.GetRecommendations

<form asp-action="Recommendations" asp-controller="EventCatalog" method="post" class="container mt-5">
    <div class="form-group">
        <label for="Artist">Artist name</label>
        <input type="text" class="form-control" id="Artist" asp-for="Artist" placeholder="Enter artist name">
    </div>
    <br />
    <button type="submit" class="btn btn-primary">Submit</button>
</form>